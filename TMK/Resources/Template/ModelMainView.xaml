<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:TMK.Screen.ViewModels"
                    xmlns:view="clr-namespace:TMK.Screen.Pipe.Views"
                    >


    <!--<DataTemplate DataType="{x:Type vm:TubesViewModel}">
        <view:TubesView DataContext="{Binding}"/>
    </DataTemplate>-->
    <!--
    <DataTemplate DataType="{x:Type vm:ManageCafeViewModel}">
        <view:ManageCafeView DataContext="{Binding}"/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type vm:WarehouseViewModel}">
        <view:WarehouseView DataContext="{Binding}"/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type vm:OrderViewModel}">
        <view:OrderView DataContext="{Binding}"/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type vm:ReportViewModel}">
        <view:ReportView DataContext="{Binding}"/>
    </DataTemplate>
    -->

</ResourceDictionary>